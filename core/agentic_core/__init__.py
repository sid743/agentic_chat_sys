"""Agentic chat platform core.

A multi-agent HR assistant (orchestrator + specialist agents) exposed through an
OpenAI-compatible API so that LibreChat (or any OpenAI-style client) can use it
as a custom endpoint.
"""

__version__ = "0.1.0"
